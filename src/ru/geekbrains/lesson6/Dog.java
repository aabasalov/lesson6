package ru.geekbrains.lesson6;

public class Dog extends Animal {
    public Dog(String name, int age, String color, double maxRunDistance, double maxSwimDistance, double maxJumpHeight, int appetite, boolean satiety) {
        super(name, age, color, maxRunDistance, maxSwimDistance, maxJumpHeight, appetite, satiety);
    }
}
